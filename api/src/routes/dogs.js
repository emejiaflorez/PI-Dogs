const { Router } = require('express');
const router = Router();
// const { addDog, getDogs, getDogById } = require('../funciones/fn_dogs.js')

// router.post("/add", addDog)
// router.get("/", getDogs)
// router.get("/:id", getDogById)

module.exports = router;